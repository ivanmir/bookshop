const exec = module.exports = {}

for (let each of ['compile', 'deploy', 'env', 'run', 'serve']) {
    exec[each] =(argv=[], options={}) => {
        if (typeof argv === 'string')  argv = [ argv ]
        return require ('./'+each) (argv,options)
    }
}